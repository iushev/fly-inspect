import React, { Component } from 'react';
import Layout from '../../hoc/Layout/Layout';

class Dashboard extends Component {
    render() {
        return (
            <Layout>
                <h1>Dashboard <small>proba</small></h1>
            </Layout>
        );
    }
}

export default Dashboard;
